package ru.unn.agile.PersonalFinance.view;

import org.junit.Ignore;
import org.junit.Test;

import static org.junit.Assert.fail;

public class FakeViewTests {
    @Test @Ignore
    public void fakeTest() throws Exception {
        fail("The fake test to prevent Travis failing (dirty hack)");
    }
}
